# Smart Legal Document Manager - Dashboard

Frontend React application built with Vite and TypeScript.

## Setup
1. `npm install`
2. `npm run dev`

Runs on port 5173 by default. Ensure the backend FastAPI server is running on `http://localhost:8000`.
